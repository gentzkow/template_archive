# Each line of instruction should contain a symlink and target delimited by a `|`
# Lines beginning with # are ignored 

# Symlink  | Target
tables.txt | {root}/analysis/output/tables.txt
plot.eps   | {root}/analysis/output/plot.eps
